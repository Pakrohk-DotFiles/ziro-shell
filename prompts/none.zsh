#!/usr/bin/env zsh
# ziro-shell — prompt: none (neutrality mode)
# Layer: T1 (Prompt Backend)
# Spec: 006
# AUTO-GENERATED: no
#
# Ziro does NOT touch PS1. User keeps their shell default.

# Intentionally empty — no PROMPT modification.
typeset -g ZIRO_PROMPT_NAME="none"
typeset -g ZIRO_PROMPT_THEME_SUPPORTED=false
